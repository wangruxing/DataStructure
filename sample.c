#include <stdio.h>
#include <stdlib.h> /* 亂數相關函數 */
#include <time.h>   /* 時間相關函數 */
#include <string.h>
#define COMPARE(x, y) ((x < y) ? -1: (x == y)? 0: 1)

// example
int main(void) {
    return 0;
}